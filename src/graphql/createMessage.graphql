mutation createMessage (
    $firstName: String!
    $lastName: String!
    $email: String!
    $content: String!
) {
    createMessage (
        firstName: $firstName
        lastName: $lastName
        email: $email
        content:$content
    )
}
